server:
  port: 8888

spring:
  application:
    name: Config-Server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EduardoSantAna1313/microservices/greeting-service-config